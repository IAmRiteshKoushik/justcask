# Big-endian record contract

The learner chose big-endian encoding for fixed-width record fields. They have now established the v1 field order, 4-byte CRC-32, 8-byte timestamp field, 4-byte lengths, checksum coverage, and non-destructive corruption behavior. Timestamp semantics and operational limits remain intentionally open before implementation.
