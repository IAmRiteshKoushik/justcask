# Big-endian record contract

The learner chose big-endian encoding for fixed-width record fields. They have now established the v1 field order, CRC-32/IEEE, an 8-byte signed Unix-nanosecond timestamp, 4-byte lengths, checksum coverage, and non-destructive corruption behavior. Operational limits remain intentionally open before implementation.
